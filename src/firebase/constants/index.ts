export enum FibaseErrorCode {
    ReSignup = 'auth/email-already-in-use',
    InvalidCredential = 'auth/invalid-credential',
    InvalidEmail = 'auth/invalid-email'
}

export const SIGNUP_SUCCESS_TYPE = 'signIn'
